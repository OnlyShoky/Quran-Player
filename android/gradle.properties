org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable file system watching to prevent incremental cache lock issues on Windows
org.gradle.unsafe.watch-fs=false
# Disable Kotlin incremental compilation - crashes on Windows paths with spaces ("Mobile apps")
kotlin.incremental=false
kotlin.incremental.java=false
# Disable Gradle daemon to prevent file lock issues on Windows
org.gradle.daemon=false
# Disable build cache to prevent stale transform artifacts
org.gradle.caching=false


